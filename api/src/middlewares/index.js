const login = require("./login");
// const registerUser = require("./registerUser");
const {
  getProyectName,
  getProyect,
  logout
} = require("./proyect");

module.exports = {
  login,
  getProyectName,
  getProyect,
  logout
};
